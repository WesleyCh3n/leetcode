impl Solution {
    pub fn combination_sum(candidates: Vec<i32>, target: i32) -> Vec<Vec<i32>> {
        todo!()
    }
}

pub struct Solution;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn test() {
        Solution {};
        pub fn combination_sum(
            candidates: Vec<i32>,
            target: i32,
        ) -> Vec<Vec<i32>> {
            todo!()
        }
    }
}
